There is a 'requirements' file in 'Codes' folder.

Go to the terminal and type 
pip install -r requirements.txt
then hit enter.

*Put your images(from which you want to extract texts) in 'Resources' folder.
*Introduce your image path in main.py file
*Keep 'colors.csv' file in 'Resources' folder.


Then run 'main.py' file.

(*) Double Click on any point of the picture and the RGB value of that point will be displayed.

Thanks.